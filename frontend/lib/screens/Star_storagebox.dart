import 'package:flutter/material.dart';

class Star_storagebox extends StatelessWidget {
  const Star_storagebox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('별 보관함');
  }
}
